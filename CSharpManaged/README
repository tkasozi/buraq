rm -r .\ManagedLibraryPublish

# In the ManagedLibrary project directory
dotnet publish Buraq.Bridge.csproj -c Release -r win-x64 --no-self-contained -o ".\ManagedLibraryPublish"

# clean

dotnet clean
